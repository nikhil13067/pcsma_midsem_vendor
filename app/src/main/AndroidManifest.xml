<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nikhil.aircanteen_vendor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".BaseNavigationDrawer"
            android:label="@string/title_activity_base_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PendingOrdersCardActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".OrderActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CompleteOrdersActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".UsersListActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".UserProfileActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".AddItemActivity"></activity>
    </application>

</manifest>